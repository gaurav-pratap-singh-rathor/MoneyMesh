spring.application.name=api-gateway
server.port=8765

#This service will get registered with the naming server.
eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka

# we need these properties only when we didn't add the custom routes in Configuration but as now we had the custom routes ,so we don't need it anymore.
#spring.cloud.gateway.discovery.locator.enabled=true
#spring.cloud.gateway.discovery.locator.lowerCaseServiceId=true

#spring.sleuth.sampler.probability=1.0
#management.tracing.sampling.probability=1.0
#logging.pattern.level=%5p [${spring.application.name:},%X{traceId:-},%X{spanId:-}]

#spring.config.import=optional:configserver:
